# 날씨 데이터를 활용한 스마트 태양광 패널 관리

## 목표
실시간 날씨 데이와 예보를 활용하여 단순한 모니터링을 넘어, 태양광 패널 관리자에게 **행동 가능한 관리 인사이트(Actionable Insight)**를 제공합니다. 이를 통해 **예지 보전(Predictive Maintenance)**과 **운영 최적화**를 달성하는 것이 목표입니다.

## 현재 상태
- ✅ **기상청 API 연동**: `WeatherService`가 초단기실황(현재 날씨) 및 단기예보(3일 예보)를 조회하고 있습니다.
- ✅ **위치 기반 매핑**: 패널의 위도/경도(`Panel` 모델)를 기상청 격자 좌표(X, Y)로 자동 변환합니다.
- ✅ **기본 로직**: `check_rain_status` 함수를 통해 비가 오면 오염 탐지 알림을 스킵하는 기본 로직이 구현되어 있습니다.

## 제안 기능

### 1. 스마트 운영 인사이트 (Actionable Alerts)
단순히 "비가 옵니다"라고 알려주는 것을 넘어, 구체적인 행동 가이드를 제공합니다.

- **자연 세척(Natural Cleaning) 추천**
    - *상황*: 내일 많은 양의 비가 예보되어 있음.
    - *인사이트*: "오늘 패널 청소는 권장하지 않습니다. 내일(날짜) 예보된 강한 비로 인해 자연 세척 효과가 기대됩니다."
- **제설 작업 알림**
    - *상황*: 눈이 오거나 예보됨.
    - *인사이트*: "패널 위에 눈이 쌓일 수 있습니다. 발전 효율 저하를 막기 위해 제설 일정을 계획하세요."
- **발전 효율 저하 경고**
    - *상황*: 짙은 구름이나 지속적인 우천.
    - *인사이트*: "기상 악화로 인해 오늘 예상 발전량이 평소보다 낮을 수 있습니다."

### 2. 대시보드 통합 (프론트엔드 연동)
- **실시간 날씨 위젯**: 패널 상태 옆에 현재 날씨 아이콘과 기온 표시.
- **3일 예보 타임라인**: 향후 3일간의 날씨 변화를 직관적인 아이콘으로 표시.
- **관리 배너**: 긴급한 인사이트(예: 태풍 경보, 폭설 주의)를 상단에 배너 형태로 노출.

### 3. 백엔드 구현 계획

#### 1단계: `WeatherStatusResponse` 확장
기존 `/weather/status` 엔드포인트 또는 새로운 `/weather/insights` 엔드포인트를 만들어 구조화된 조언 코드를 반환합니다.

```python
class WeatherInsight(BaseModel):
    type: str # 'cleaning'(청소), 'maintenance'(유지보수), 'efficiency'(효율)
    level: str # 'info'(정보), 'warning'(주의), 'action_required'(조치필요)
    message: str
    action_item: Optional[str] # 구체적인 행동 제안
```

#### 2단계: 백그라운드 날씨 모니터링
현재 로직은 사용자가 API를 호출할 때만 날씨를 조회합니다.
- **스케줄러 작업**: 주기적(예: 1시간마다)으로 모든 활성 패널 지역의 날씨를 확인.
- **푸시 알림**: 우박, 폭설 등 패널에 물리적 타격을 줄 수 있는 기상 악화 시 관리자에게 즉시 푸시 알림 전송.

## 사용자 검토 필요 사항
- **알림 정책**: 작은 비소식에도 매번 알림을 보낼지, 아니면 '호우 주의보' 급 이상의 기상 악화 시에만 보낼지 결정이 필요합니다.
- **자동화 수준**: 비가 올 때 오염 탐지 프로세스를 자동으로 "일시 정지"시켜 서버 리소스를 아낄까요? (현재 일부 구현됨)

## 다음 단계
1.  `WeatherService`에 **"인사이트(Insight) 분석 로직"** 구현.
2.  해당 정보를 반환하는 새로운 API 엔드포인트 설계.
3.  (프론트엔드) 패널 상세 페이지에 해당 정보를 시각화.
